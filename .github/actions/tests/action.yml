name: Tests
description: Run all tests

runs:
  using: "composite"
  steps:
    - name: Pytest
      shell: bash
      run: |
        uv run coverage run -m pytest
        uv run coverage report -m --skip-covered
